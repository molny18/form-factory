{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('homepage') }}">INDEX</a>
    <h1>Formfieldinstance creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('formfieldinstance_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
